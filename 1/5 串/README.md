

### 串



### 串的抽象数据类型

```
    Data
        s
    
    Operation
        StrAssign(T, *chars)
        StrCopy(T, S)
        ClearString(S)
        StringEmpty(S)
        StrLength(S)
        StrCompare(S, T)
        Concat(T, S1, S2)
        SubString(Sub, S, pos, len)
        Index(S, T, pos)
        Replace(S, T, V)
        StringInsert(S. pos, T)
        StrDelete(S, pos, len)
```